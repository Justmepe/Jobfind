# Local state and secrets — never commit these
config.json
seen_jobs.json
__pycache__/
*.pyc
.venv/

# Machine-specific local runner + logs
run_jobwatch.cmd
launch_hidden.vbs
*.log

# Personal profile (may hold an SMTP app password) + generated output
profile.json
applications_queue.json
applications/

# Resume / any Word docs — personal, never commit
*.docx
